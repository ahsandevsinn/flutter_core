import 'package:flutter/material.dart';

class Navigator extends StatelessWidget {
  const Navigator({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(children: [
//  Navigator.push(
//   context,
//   MaterialPageRoute(builder: (context) => SecondPage()),
// );
  
// navigator.push stack based approach use karta hai ismay screen new stack may add hoti hai like stack may screen ki oper neew screeen hi

// Navigator.pop(context);
// Navigator.pop(context); current stack may say screen ko remove kkarta hai oh
    ],);
  }
}